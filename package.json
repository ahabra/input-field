{
  "name": "@techexp/input-field",
  "version": "1.6.0",
  "description": "Web Component. Responsive input field with label and validation",
  "author": "Abdul Habra",
  "license": "Apache",
  "repository": "ahabra/input-field",
  "main": "dist/input-field-esm.js",
  "engines": {
    "node": ">=15.0.0",
    "npm": ">=7.0.0"
  },
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "node scripts/build.mjs",
    "build:prod": "node scripts/build.mjs prod",
    "server": "node scripts/server.mjs",
    "test": "npm run lint && web-test-runner",
    "test:watch": "web-test-runner --watch",
    "lint": "eslint src --fix",
    "deps:list": "ncu",
    "deps:up": "ncu -u"
  },
  "devDependencies": {
    "@esm-bundle/chai": "^4.3.4",
    "@web/test-runner": "^0.18.0",
    "@web/test-runner-commands": "^0.9.0",
    "@web/test-runner-puppeteer": "^0.15.0",
    "chalk": "^5.3.0",
    "esbuild": "^0.19.10",
    "eslint": "^8.56.0",
    "fs-extra": "^11.2.0",
    "npm-check-updates": "^16.14.12",
    "serve": "^14.2.1"
  },
  "dependencies": {
    "@techexp/jshelper": "^0.6.3",
    "@techexp/webitem": "^0.5.3"
  }
}
